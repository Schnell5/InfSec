font = ('courier', 14, 'normal')

bg = 'lightcyan'
fg = 'black'

height = 60
width = 120

caseinsens = True

opensAskUser = True  # Ask user to choose encoding if the encoding is not known yet
opensEncoding = ''   # Try to use this encoding if user didn't choose any encoding in the previous step

savesUseKnownEncoding = 1   # If > 0 then try to use encoding that was used at the previous Open or Save
savesAskUser = True         # Ask user to choose the encoding during save
savesEncoding = ''          # Try to use these encodings if user didn't choose any encoding in the previous step
